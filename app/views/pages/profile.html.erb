<div class="contenair">
  <div class="row justify-content-center">
    <br><br><br><br>
    Nom : <%= current_user.last_name %><br>
    Prénom : <%= current_user.first_name %><br>
    Numéro de tel : <%= current_user.phone_number %><br>
    Compétence : <%= current_user.skills %><br>
    Information personnelle :<%= current_user.biography %><br>
    Localisation : <%= current_user.address %><br>
  </div>

  <div class="row justify-content-center">
    <div class="col-6">
      <div class="profile-map-container">
        <div style="width: 100%; height: 100%;"
              data-controller="map"
              data-map-markers-value="<%= @markers.to_json %>"
              data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
        </div>
      </div>
    </div>
  </div>
</div>
